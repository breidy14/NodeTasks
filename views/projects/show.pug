extends ../layout

block contenido

    h1= project.name
    p= project.createdAt
    
    a(href=`/projects/${project.id}/edit` class="btn btn-info" role="button") Editar
    form(action=`/projects/${project.id}?_method=DELETE` method="POST")
        input(type="submit" value="Eliminar")